# Archive

Legacy SpiralOS artifacts live here for historical reference only. Code under `archive/legacy/` is not loaded by the active orchestrator and should not be extended. When you need older behavior, port it into `core/spiralos.py` instead of editing the archived files directly.
